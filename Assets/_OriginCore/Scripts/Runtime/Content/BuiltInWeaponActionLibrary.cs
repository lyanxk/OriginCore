namespace OriginCore.Content
{
    // Retains the established type/name as a compatibility surface for stable IDs.
    // Action definitions now live exclusively in serialized WeaponActionSet assets.
    public static class BuiltInWeaponActionLibrary
    {
        public const string TimeslotWeaponId = "weapon.y.timeslot-key";
        public const string SequenceWeaponId = "weapon.y.sequence-key";
        public const string FinalWingWeaponId = "weapon.y.final-wing";
    }
}
